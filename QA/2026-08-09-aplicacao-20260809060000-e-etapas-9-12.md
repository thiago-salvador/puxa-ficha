# QA: aplicação da 20260809060000 e etapas 9 a 12 (12 match_fresco)

Data: 2026-08-09, noite. Autorização nomeada do dono na mesma conversa:
"Aplicar a migration 20260809060000 em produção e rodar as etapas 9 a 12 para
os 12 confirmados (escrita auditada, invalidação de cache e leitura pública
comprovada)", aprovada com "Sim, autorizo".

## R1: migration de schema

| Prova | Resultado |
|---|---|
| Readback pré | ledger 370, topo `20260809052600`, coluna ausente, 12 slugs presentes e publicáveis (snapshot em `output/pf-reverificacao-20260809/etapa-9-snapshot-pre.json`) |
| Sonda de canal | tabela-sonda visível na transação (gate `DO`), ausente pós-rollback (0) |
| Dry-run | migration inteira + linha do ledger em `BEGIN … ROLLBACK`; gate interno viu 371 / topo novo / coluna na tabela e na view; pós-rollback 370 / topo antigo / coluna 0 |
| Aplicação | DDL + `INSERT` no ledger na MESMA transação; `statements` sem comentários, elemento único, `created_by` do dono, formato da precedente |
| Readback pós | ledger **371**, topo `20260809060000`, coluna na tabela e na view, **0 linhas com dado** (schema puro) |
| Ledger guard | `audit:ledger:gate` com a lista remota pós-aplicação: "ledger e repositório contam a mesma história", 5 retidas reconhecidas |

## Etapa 9: materialização dos 12

- Script novo `scripts/materializar-etapa9-tse-12.ts` (não commitado): universo
  derivado do registro versionado, porta `exigirMaterializacaoTse2026` por slug,
  data = `decidido_em` (`2026-08-09`), redes de `felicio-ramuth`
  (`social_count: 0`) como `vazio_confirmado`, merge aditivo sem null.
- Dry-run conferido antes do `--apply`. Escrita: **12 de 12**, cada uma tocando
  exatamente 1 linha, via `escreverAuditado()` com preflight de trilha.

## Etapa 10: reconciliação

12 linhas de domínio com o patch exato, 12 de trilha
(`fonte='escrita:materializar-etapa9-tse-12'`), volume 12, conjuntos idênticos
nos dois sentidos (0 sem trilha, 0 sem domínio).

## Etapa 11: invalidação e readback público

- `revalidate-cache.yml` run 31335012032, success (tags de ficha).
- Readback dos 83: **12 em `current` com "Perfil verificado em 08/08/2026 (TSE
  candidaturas 2026)"**, 71 intactos nas datas curadas anteriores, 0 falhas.
  Evidência: `output/pf-reverificacao-20260809/etapa-11-readback.json`.

## Etapa 12

- `audit:cobertura` reexecutado contra produção, 194 publicáveis, snapshot novo.
- `Settings/STATUS.md` com a seção "Snapshot 09/08/2026 (noite)".
- Recheque dos 43 agendado para 16/08 09:00 (scheduled task
  `recheque-tse-43-pf`), depois do fechamento da janela do TSE (15/08 19h).

## Defeito anotado, não corrigido aqui

Data pura em `verificacao_campos` é ancorada em meia-noite UTC e exibida em
`America/Sao_Paulo`: gravado `2026-08-09`, exibido `08/08/2026`. O exibido
coincide com a data do snapshot do TSE, então a superfície não mente, mas o
leitor deveria preservar a semântica de data de calendário. Chip de task aberto.

## Pendências que continuam abertas

Recheque dos 43 (16/08), 12 `revisao_identidade` aguardando confirmação por
chave independente, migration retida `20260807052000`, os 2 `social_networks`
de `cleber-rabelo`/`gilberto-vasconcelos`, allowlist FAIL 550 (baseline
histórico), e o commit deste recibo, do script da etapa 9 e do recibo anterior
(ato próprio, via branch).
